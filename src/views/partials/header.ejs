<header>
    <nav>
      <ul class="nav-left">
        <li>
          <a href="/app/home" class="logo-container">
            <img src="./images/LogoGuevara.png" alt="LogoGuevara" class="logo" />
            <span class="logo-text">Colegio Dr. Ernesto Guevara</span>
          </a>
        </li>
      </ul>
    </nav>
    <ul class="nav-right">
      <li>
        <a href="/app/perfil" class="profile-container">
          <img
            src="./images/usuarios/<%= usuario.foto_perfil %>"
            alt="Foto de Perfil"
            class="profile-image"
          />
          <span class="user-name"><%= usuario.nombre %></span>
        </a>
      </li>
    </ul>
  </header>
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
  
    ul {
      list-style-type: none; 
    }
  
    header {
      width: 100%;
      background-color: #d13030;
      padding: 15px 20px;
      position: fixed;
      top: 0;
      left: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 1000;
      box-sizing: border-box;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    }
  
    /* Logo y texto del header */
    .nav-left {
      display: flex;
      align-items: center;
    }
  
    .logo-container {
      display: flex;
      align-items: center;
      text-decoration: none;
      transition: all 0.3s ease;
      cursor: pointer;
    }
  
    .logo {
      height: 3rem;
      width: auto;
      margin-right: 10px;
      border-radius: 50%;
      transition: transform 0.3s ease;
    }
  
    .logo:hover {
      transform: scale(1.1);
    }
  
    .logo-text {
      font-size: 20px;
      color: white;
      font-weight: bold;
      letter-spacing: 1px;
      text-align: center;
      transition: color 0.3s ease;
    }
  
    .logo-container:hover .logo-text {
      color: #000000; /* Cambia a un color dorado en hover */
    }
  
    .logo-container:active .logo-text {
      transform: scale(0.95); /* Simula un efecto de clic */
    }
  
    /* Navegación derecha */
    .nav-right {
      display: flex;
      align-items: center;
    }
  
    .profile-container {
      display: flex;
      align-items: center;
      text-decoration: none;
      color: white;
      transition: transform 0.3s ease, color 0.3s ease;
      cursor: pointer;
    }
  
    .profile-container:hover {
      transform: scale(1.05);
      color: #000000;
    }
  
    .profile-container:active {
      transform: scale(0.95);
    }
  
    /* Imagen de perfil */
    .profile-image {
      width: 45px;
      height: 45px;
      object-fit: cover;
      border-radius: 50%;
      border: 2px solid white;
      margin-right: 10px;
      transition: transform 0.3s ease;
    }
  
    .user-name {
      font-size: 16px;
      color: white;
      font-weight: 600;
      white-space: nowrap;
      transition: color 0.3s ease;
    }
  
    /* Responsividad */
    @media (max-width: 768px) {
      header {
        flex-direction: column;
        text-align: center;
      }
  
      .nav-left,
      .nav-right {
        flex-direction: column;
        align-items: center;
      }
  
      .logo-text {
        font-size: 18px;
      }
  
      .profile-image {
        width: 40px;
        height: 40px;
      }
  
      .user-name {
        font-size: 14px;
      }
    }
  
    @media (max-width: 480px) {
      header {
        padding: 10px;
      }
  
      .logo {
        height: 2.5rem;
      }
  
      .logo-text {
        font-size: 16px;
      }
  
      .profile-image {
        width: 35px;
        height: 35px;
      }
  
      .user-name {
        font-size: 12px;
      }
    }
  </style>
  